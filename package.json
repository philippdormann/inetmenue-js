{
  "version": "0.1.9",
  "private": true,
  "scripts": {
    "release": "bumpp package.json packages/*/package.json --all",
    "ci:publish": "zx scripts/publish.mjs",
    "start": "pnpm -C packages/api run start",
    "dev": "pnpm -C packages/api run dev"
  },
  "devDependencies": {
    "bumpp": "^8.2.1",
    "zx": "^7.1.1"
  }
}
